- It has naming convention to start with `I`.
- Interfaces is primarily for making our code loosely coupled
- It doesn't have implementation for methods nor access modifiers.
```c#
public interface ITaxCalculator
{
	int Calculate();
}
```