- Its actual name is `maven-clean-plugin`

| Goal  | Descritpion                  |
| ----- | ---------------------------- |
| clean | deletes the target directory |


> [!Attention]
> Using `clean` only describes the clean phase in lifeCycle, but `clean plugin` refere to this plugin that run in clean lifecycle.
