- Filter is a class that runs before controllers and can inspect or modify http requests. Comman usage are: **Check for authentication**, **log incoming Requests**, **Modify headers**, **Block suspicious traffic**.
- Your custom filters will run even if the endpoint is `.permitAll()`.