- The main idea is to use private fields instead of public so we don't do direct interaction with field, we should interact with it through setters, getters. This ensure our fields don't enter in invalid state.
- Most of the times you will find that attributes will be private and methods are public.