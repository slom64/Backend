- Annotations works as metadata, and it get processed later "Ex from compiler or runtime".
- They can be applied to classes, methods, variables, another annotation.

```java
@SuppressWarnings("unused")
Cat myCat = new Cat("Stella");
```