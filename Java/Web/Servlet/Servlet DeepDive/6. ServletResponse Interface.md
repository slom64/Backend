- The SerlvetResponse interface defines an object that carries the server's response to the user ( i.e. back to the client's browser)
- Commonly used methods:
	- `PrinterWriter getWriter()`
	- `setContentType(String mimeType)`
	- `setContentLenght(int len)`